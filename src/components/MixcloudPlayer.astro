---
type Props = {
  mixcloudKey: string;
  title: string;
};

const { mixcloudKey, title } = Astro.props;
// light=0 keeps the embed on its dark theme so it sits in the page.
const src = `https://www.mixcloud.com/widget/iframe/?hide_cover=1&light=0&feed=${encodeURIComponent(
  mixcloudKey
)}`;
---

<iframe
  class="mixcloud-player"
  title={`Mixcloud player for ${title}`}
  src={src}
  width="100%"
  height="120"
  frameborder="0"
  loading="lazy"
  allow="autoplay"
></iframe>
